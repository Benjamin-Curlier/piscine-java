package etnc.m5;

/**
 * Exercice 5.1.3 — Calculs géométriques avec gardes (starter).
 *
 * <p>Refactor de l'exercice 2.3.3 : on ajoute des gardes sur les dimensions
 * négatives. Chaque méthode doit lever une {@link IllegalArgumentException} si
 * une dimension est strictement négative (le zéro est autorisé).</p>
 *
 * <p>Complétez les quatre méthodes. Ne modifiez pas leurs signatures.</p>
 */
public class CalculsGeometriques {

    /** Aire d'un cercle de rayon donné. Lève IAE si rayon < 0. */
    public static double aire(double rayon) {
        return 0; // TODO
    }

    /** Aire d'un rectangle de largeur et hauteur données. Lève IAE si une dimension < 0. */
    public static double aire(double largeur, double hauteur) {
        return 0; // TODO
    }

    /** Périmètre d'un cercle de rayon donné. Lève IAE si rayon < 0. */
    public static double perimetre(double rayon) {
        return 0; // TODO
    }

    /** Périmètre d'un rectangle de largeur et hauteur données. Lève IAE si une dimension < 0. */
    public static double perimetre(double largeur, double hauteur) {
        return 0; // TODO
    }
}
