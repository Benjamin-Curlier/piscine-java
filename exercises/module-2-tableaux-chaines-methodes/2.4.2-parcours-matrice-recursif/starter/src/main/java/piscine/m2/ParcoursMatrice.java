package piscine.m2;

/**
 * Exercice 2.4.2 — Parcours RECURSIF d'une matrice.
 *
 * Completez sommeMatrice : elle doit calculer la somme de tous les elements
 * de la matrice de maniere RECURSIVE (cas de base + appel recursif). Vous
 * pouvez (et devriez) ajouter vos propres methodes private recursives
 * auxiliaires. Testee par valeur de retour.
 */
public class ParcoursMatrice {

    /** Somme de tous les elements de la matrice (recursif). */
    public static int sommeMatrice(int[][] matrice) {
        // TODO (recursif) : par exemple, additionner la somme de la 1re ligne
        // et la somme du reste de la matrice ; cas de base = plus de ligne.
        // Pensez a une methode auxiliaire recursive pour sommer une ligne.
        return 0;
    }
}
