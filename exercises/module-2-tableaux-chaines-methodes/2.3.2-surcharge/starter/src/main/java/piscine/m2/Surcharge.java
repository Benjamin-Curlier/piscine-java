package piscine.m2;

/**
 * Exercice 2.3.2 — Surcharge de methodes.
 *
 * Completez les trois methodes maximum. Elles portent le meme nom mais des
 * parametres differents (c'est la surcharge). Testees par valeur de retour.
 */
public class Surcharge {

    /** Le plus grand de deux entiers. */
    public static int maximum(int a, int b) {
        // TODO : renvoyer le plus grand de a et b
        return 0;
    }

    /** Le plus grand de trois entiers. */
    public static int maximum(int a, int b, int c) {
        // TODO : renvoyer le plus grand de a, b et c
        return 0;
    }

    /** Le plus grand de deux nombres a virgule. */
    public static double maximum(double a, double b) {
        // TODO : renvoyer le plus grand de a et b
        return 0.0;
    }
}
