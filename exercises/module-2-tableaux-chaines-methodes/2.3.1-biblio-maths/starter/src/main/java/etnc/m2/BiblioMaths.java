package etnc.m2;

/**
 * Exercice 2.3.1 — Bibliotheque mathematique.
 *
 * Completez les trois methodes static. Elles sont testees par leur valeur de
 * retour (pas d'affichage).
 */
public class BiblioMaths {

    /** Plus grand commun diviseur de a et b (entiers >= 0). */
    public static int pgcd(int a, int b) {
        // TODO : algorithme d'Euclide (pgcd(a, 0) = a)
        return 0;
    }

    /** Vrai si n est un nombre premier (n < 2 -> false). */
    public static boolean estPremier(int n) {
        // TODO : tester si n n'a aucun diviseur entre 2 et n-1 (ou racine de n)
        return false;
    }

    /** Somme des chiffres de n (n >= 0). */
    public static int sommeChiffres(int n) {
        // TODO : additionner les chiffres (utiliser % 10 et / 10)
        return 0;
    }
}
