# --- Java / Maven ---
target/
*.class
*.jar
*.war
*.ear
*.nar
hs_err_pid*
replay_pid*
# Le wrapper Maven est volontairement versionné (build offline / `./mvnw` reproductible, cf. #54)
!.mvn/wrapper/maven-wrapper.jar
# Régénéré par maven-shade-plugin à chaque `mvn package`
**/dependency-reduced-pom.xml

# --- Gradle ---
.gradle/
# Le wrapper Gradle est volontairement versionné (build offline reproductible, même politique que #54)
!moulinette/gradle/wrapper/gradle-wrapper.jar
# (build/ est déjà ignoré plus bas, section Docusaurus)

# --- IDE ---
.idea/
*.iml
*.iws
*.ipr
out/
.vscode/
.settings/
.project
.classpath
.factorypath
.apt_generated/
.springBeans
.sts4-cache

# --- Docusaurus / Node ---
node_modules/
.docusaurus/
build/
.cache-loader/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- OS ---
Thumbs.db
Desktop.ini
ehthumbs.db
.DS_Store
.AppleDouble
.LSOverride

# --- Logs / temp ---
logs/
*.tmp
*.swp
*~

# --- Secrets ---
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# --- Moulinette runtime ---
moulinette/**/work/
moulinette/**/reports-output/

# --- Bundle stagiaire (artefact de build, contient un JDK) ---
dist/

# --- Claude Code local user settings (per-user, not shared) ---
.claude/settings.local.json

