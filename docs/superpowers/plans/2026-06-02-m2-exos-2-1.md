# Sous-groupe 2.1 (Tableaux) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: this project executes plans **inline** (contrainte projet : pas de sous-agents). Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Créer les 4 exercices du sous-groupe 2.1 (`min-max-moyenne`, `inversion`, `recherche-lineaire`, `rotation-matrice`) sous `exercises/module-2-tableaux-chaines-methodes/`, chacun complet (11 fichiers), solution de référence validée en local.

**Architecture:** Chaque exercice est un trio de projets Maven (`starter/`, `tests/` + `tests-prives/` injectés, `solution/`). Le stagiaire écrit tout dans `main`, lit l'entrée via `Scanner(System.in)`. Les tests injectent l'entrée avec `CaptureEntree` et lisent la sortie avec `CaptureSortie`. La **solution de référence qui passe tests publics + privés** est le filet de validation du format.

**Tech Stack:** Java 25, Maven (build-helper pour injecter les tests), JUnit Jupiter 5.11.0, AssertJ 3.26.3. Package `etnc.m2`, groupId `etnc.piscine.m2`.

**Spec de référence :** [`docs/superpowers/specs/2026-06-02-m2-exos-2-1-design.md`](../specs/2026-06-02-m2-exos-2-1-design.md). Branche : `feature/m2-exos-2-1` (déjà créée).

---

## Arborescence cible

```
exercises/module-2-tableaux-chaines-methodes/
├── 2.1.1-min-max-moyenne/
├── 2.1.2-inversion/
├── 2.1.3-recherche-lineaire/
└── 2.1.4-rotation-matrice/
```

Chaque dossier `2.1.E-slug/` contient **exactement** ces 11 fichiers :
```
sujet.md
metadata.yml
evaluation.yml
correction.md
starter/pom.xml
starter/src/main/java/etnc/m2/<Classe>.java
tests/src/test/java/etnc/m2/<Classe>Test.java
tests/src/test/java/etnc/util/CaptureSortie.java
tests/src/test/java/etnc/util/CaptureEntree.java
tests-prives/src/test/java/etnc/m2/<Classe>PriveTest.java
solution/pom.xml
solution/src/main/java/etnc/m2/<Classe>.java
```

Mapping slug → classe → artifactId :

| Exo | slug | Classe | artifactId starter | artifactId solution |
|---|---|---|---|---|
| 2.1.1 | min-max-moyenne | `MinMaxMoyenne` | `min-max-moyenne` | `min-max-moyenne-solution` |
| 2.1.2 | inversion | `Inversion` | `inversion` | `inversion-solution` |
| 2.1.3 | recherche-lineaire | `RechercheLineaire` | `recherche-lineaire` | `recherche-lineaire-solution` |
| 2.1.4 | rotation-matrice | `RotationMatrice` | `rotation-matrice` | `rotation-matrice-solution` |

---

## Blocs partagés (réutilisés tels quels par les 4 exos)

Ces fichiers sont **identiques** d'un exo à l'autre, à la substitution près indiquée. Ne pas les ré-inventer : copier ces blocs.

### BLOC-A — `tests/src/test/java/etnc/util/CaptureSortie.java`
Copier **verbatim** depuis `exercises/module-1-fondamentaux/1.1.3-lecture-saisie/tests/src/test/java/etnc/util/CaptureSortie.java`. (Contenu identique pour les 4 exos, aucune modification.)

Commande de copie (à adapter par exo) :
```bash
mkdir -p exercises/module-2-tableaux-chaines-methodes/2.1.E-slug/tests/src/test/java/etnc/util
cp exercises/module-1-fondamentaux/1.1.3-lecture-saisie/tests/src/test/java/etnc/util/CaptureSortie.java \
   exercises/module-2-tableaux-chaines-methodes/2.1.E-slug/tests/src/test/java/etnc/util/CaptureSortie.java
cp exercises/module-1-fondamentaux/1.2.1-conversion-unites/tests/src/test/java/etnc/util/CaptureEntree.java \
   exercises/module-2-tableaux-chaines-methodes/2.1.E-slug/tests/src/test/java/etnc/util/CaptureEntree.java
```

### BLOC-B — `tests/src/test/java/etnc/util/CaptureEntree.java`
Copier **verbatim** depuis `exercises/module-1-fondamentaux/1.2.1-conversion-unites/tests/src/test/java/etnc/util/CaptureEntree.java`. (Identique pour les 4 exos.)

### BLOC-C — `starter/pom.xml`
Identique pour les 4 exos **sauf** `<artifactId>` et `<name>`. Modèle (remplacer `ARTIFACT` et `NOM`) :
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>etnc.piscine.m2</groupId>
    <artifactId>ARTIFACT</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>NOM (starter)</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <junit.version>5.11.0</junit.version>
        <assertj.version>3.26.3</assertj.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.0</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>add-public-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.basedir}/../tests/src/test/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
```

### BLOC-D — `solution/pom.xml`
Identique à BLOC-C **sauf** : `<artifactId>ARTIFACT-solution</artifactId>`, `<name>NOM (solution de reference)</name>`, et le bloc `<sources>` ajoute **en plus** la source des tests privés :
```xml
                            <sources>
                                <source>${project.basedir}/../tests/src/test/java</source>
                                <source>${project.basedir}/../tests-prives/src/test/java</source>
                            </sources>
```

### BLOC-E — `evaluation.yml`
Identique pour les 4 exos :
```yaml
total: 20
seuil_reussite: 12
criteres:
  - id: tests-publics
    description: "Tests publics passants"
    poids: 8
    type: automatique
  - id: tests-prives
    description: "Tests privés passants (cas limites)"
    poids: 8
    type: automatique
  - id: style
    description: "Style et lisibilité (Checkstyle + PMD)"
    poids: 2
    type: automatique
  - id: demarche
    description: "Pertinence de la démarche (lisibilité, idiomatisme)"
    poids: 2
    type: formateur
```

### Commandes de validation (rappel kickoff §4)
```bash
# UNE solution (remplacer le chemin) — attendu : BUILD SUCCESS, Tests run: N, Failures: 0
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" \
  -f exercises/module-2-tableaux-chaines-methodes/2.1.E-slug/solution/pom.xml test

# Vérifier que le STARTER échoue (TODO non rempli) — attendu : BUILD FAILURE (Failures > 0)
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" \
  -f exercises/module-2-tableaux-chaines-methodes/2.1.E-slug/starter/pom.xml test
```

---

## Task 1 : 2.1.1 — min-max-moyenne (gabarit du sous-groupe)

**Files (tous à créer) :**
- `exercises/module-2-tableaux-chaines-methodes/2.1.1-min-max-moyenne/sujet.md`
- `.../2.1.1-min-max-moyenne/metadata.yml`
- `.../2.1.1-min-max-moyenne/evaluation.yml` (BLOC-E)
- `.../2.1.1-min-max-moyenne/correction.md`
- `.../2.1.1-min-max-moyenne/starter/pom.xml` (BLOC-C, ARTIFACT=`min-max-moyenne`, NOM=`Min, max et moyenne`)
- `.../2.1.1-min-max-moyenne/starter/src/main/java/etnc/m2/MinMaxMoyenne.java`
- `.../2.1.1-min-max-moyenne/tests/src/test/java/etnc/m2/MinMaxMoyenneTest.java`
- `.../2.1.1-min-max-moyenne/tests/src/test/java/etnc/util/CaptureSortie.java` (BLOC-A)
- `.../2.1.1-min-max-moyenne/tests/src/test/java/etnc/util/CaptureEntree.java` (BLOC-B)
- `.../2.1.1-min-max-moyenne/tests-prives/src/test/java/etnc/m2/MinMaxMoyennePriveTest.java`
- `.../2.1.1-min-max-moyenne/solution/pom.xml` (BLOC-D, ARTIFACT=`min-max-moyenne`, NOM=`Min, max et moyenne`)
- `.../2.1.1-min-max-moyenne/solution/src/main/java/etnc/m2/MinMaxMoyenne.java`

- [ ] **Step 1 : Créer l'arborescence + copier les utils + poms + evaluation.yml**

Créer les dossiers, copier BLOC-A/BLOC-B (commande de copie ci-dessus), écrire `starter/pom.xml` (BLOC-C), `solution/pom.xml` (BLOC-D), `evaluation.yml` (BLOC-E).

- [ ] **Step 2 : Écrire les tests publics** — `tests/src/test/java/etnc/m2/MinMaxMoyenneTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests publics de l'exercice 2.1.1 Min, max et moyenne.
 */
@DisplayName("Min, max, moyenne — tests publics")
class MinMaxMoyenneTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> MinMaxMoyenne.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("cas nominal : 3 9 1 7 5")
    void cas_nominal() {
        assertThat(executer("5" + NL + "3 9 1 7 5" + NL))
            .as("min=1, max=9, moyenne=5.00")
            .isEqualTo("Min : 1" + NL + "Max : 9" + NL + "Moyenne : 5.00" + NL);
    }

    @Test
    @DisplayName("moyenne non entière formatée à 2 décimales avec un point")
    void moyenne_decimale() {
        assertThat(executer("2" + NL + "10 3" + NL))
            .as("moyenne de 10 et 3 = 6.50")
            .isEqualTo("Min : 3" + NL + "Max : 10" + NL + "Moyenne : 6.50" + NL);
    }
}
```

- [ ] **Step 3 : Écrire les tests privés** — `tests-prives/src/test/java/etnc/m2/MinMaxMoyennePriveTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests privés de l'exercice 2.1.1 — cas limites.
 */
@DisplayName("Min, max, moyenne — tests privés")
class MinMaxMoyennePriveTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> MinMaxMoyenne.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("tableau à un seul élément")
    void un_seul_element() {
        assertThat(executer("1" + NL + "42" + NL))
            .isEqualTo("Min : 42" + NL + "Max : 42" + NL + "Moyenne : 42.00" + NL);
    }

    @Test
    @DisplayName("valeurs négatives")
    void valeurs_negatives() {
        assertThat(executer("3" + NL + "-5 -1 -9" + NL))
            .isEqualTo("Min : -9" + NL + "Max : -1" + NL + "Moyenne : -5.00" + NL);
    }

    @Test
    @DisplayName("moyenne arrondie au centième (1 2 2 -> 1.67)")
    void moyenne_arrondie() {
        assertThat(executer("3" + NL + "1 2 2" + NL))
            .isEqualTo("Min : 1" + NL + "Max : 2" + NL + "Moyenne : 1.67" + NL);
    }
}
```

- [ ] **Step 4 : Écrire le starter** — `starter/src/main/java/etnc/m2/MinMaxMoyenne.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.1 — Min, max et moyenne d'un tableau d'entiers.
 */
public class MinMaxMoyenne {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        // TODO : lire n (taille), puis n entiers dans un tableau int[]
        // TODO : parcourir le tableau pour trouver le minimum et le maximum
        // TODO : calculer la moyenne (en double, sans division entière)
        // TODO : afficher exactement, une valeur par ligne :
        //   Min : <min>
        //   Max : <max>
        //   Moyenne : <moyenne à 2 décimales, séparateur point>
        // Astuce format moyenne : String.format(java.util.Locale.ROOT, "%.2f", moyenne)
    }
}
```

- [ ] **Step 5 : Vérifier que le starter ÉCHOUE** — Run :
```bash
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" \
  -f exercises/module-2-tableaux-chaines-methodes/2.1.1-min-max-moyenne/starter/pom.xml test
```
Attendu : **BUILD FAILURE**, `Tests run: 2, Failures: 2` (le starter n'affiche rien).

- [ ] **Step 6 : Écrire la solution** — `solution/src/main/java/etnc/m2/MinMaxMoyenne.java`

```java
package etnc.m2;

import java.util.Locale;
import java.util.Scanner;

/**
 * Exercice 2.1.1 — Min, max et moyenne (solution de reference).
 *
 * <p>Lit une taille puis autant d'entiers, et affiche le minimum, le maximum
 * et la moyenne. La somme est cumulee dans un {@code long} pour eviter tout
 * debordement sur de grands tableaux ; la moyenne est formatee avec un point
 * decimal (Locale.ROOT) afin que la sortie soit independante de la locale.</p>
 */
public class MinMaxMoyenne {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        int n = clavier.nextInt();

        int[] valeurs = new int[n];
        for (int i = 0; i < n; i++) {
            valeurs[i] = clavier.nextInt();
        }

        int min = valeurs[0];
        int max = valeurs[0];
        long somme = 0;
        for (int v : valeurs) {
            if (v < min) {
                min = v;
            }
            if (v > max) {
                max = v;
            }
            somme += v;
        }
        double moyenne = (double) somme / n;

        System.out.println("Min : " + min);
        System.out.println("Max : " + max);
        System.out.println("Moyenne : " + String.format(Locale.ROOT, "%.2f", moyenne));
    }
}
```

- [ ] **Step 7 : Vérifier que la solution PASSE (publics + privés)** — Run :
```bash
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" \
  -f exercises/module-2-tableaux-chaines-methodes/2.1.1-min-max-moyenne/solution/pom.xml test
```
Attendu : **BUILD SUCCESS**, `Tests run: 5, Failures: 0` (2 publics + 3 privés).

- [ ] **Step 8 : Écrire `metadata.yml`**

```yaml
slug: min-max-moyenne
titre: "Min, max et moyenne"
module: 2
sous_groupe: "2.1"
position: 1
difficulte: facile
duree_estimee_min: 25
prerequis: []
objectifs_pedagogiques:
  - "Déclarer et remplir un tableau d'entiers à partir d'une saisie"
  - "Parcourir un tableau pour accumuler minimum, maximum et somme"
  - "Calculer une moyenne en nombres à virgule et la formater proprement"
notions:
  - tableau
  - boucle
  - min-max
  - moyenne
auteur: "ETNC"
version: 1
date_creation: 2026-06-02
```

- [ ] **Step 9 : Écrire `sujet.md`**

```markdown
# Exercice 2.1.1 — Min, max et moyenne

## Contexte

Au poste de transmission, vous recevez une série de mesures (par exemple des
niveaux de signal). Avant de transmettre un compte rendu, vous devez en extraire
les valeurs caractéristiques : la plus basse, la plus haute et la moyenne.

## Énoncé

Écrivez un programme qui lit une série d'entiers, puis affiche leur minimum,
leur maximum et leur moyenne.

La saisie se fait au clavier, dans cet ordre :

1. une première ligne contient `n`, le nombre de valeurs (au moins 1) ;
2. une seconde ligne contient les `n` entiers, séparés par des espaces.

Affichez ensuite, une information par ligne :

```
Min : <minimum>
Max : <maximum>
Moyenne : <moyenne>
```

La moyenne est affichée avec **deux décimales** et un **point** comme séparateur
(par exemple `5.00` ou `6.50`).

## Exemple

**Exécution attendue** (l'utilisateur saisit `5` puis `3 9 1 7 5`) :

```text
Min : 1
Max : 9
Moyenne : 5.00
```

## Contraintes

- La classe doit s'appeler `MinMaxMoyenne` et rester dans le package `etnc.m2`.
- Lisez l'entrée avec un `Scanner`. Tout le code peut tenir dans `main`.
- La moyenne doit être calculée en **nombres à virgule** (`double`), sans
  division entière, et affichée à deux décimales avec un point
  (astuce : `String.format(java.util.Locale.ROOT, "%.2f", moyenne)`).
- Le format de sortie doit être exactement celui de l'exemple.

## Ce qui sera vérifié

- Votre programme compile et lit correctement la taille puis les valeurs.
- Le minimum et le maximum sont corrects, y compris avec des valeurs négatives.
- La moyenne est correcte et formatée à deux décimales.
- Le format des trois lignes de sortie est respecté.

## Pour aller plus loin (optionnel — non noté)

- Que se passe-t-il si la somme dépasse la capacité d'un `int` ? Renseignez-vous
  sur le type `long` et la notion de débordement (overflow).
- La classe `java.util.Arrays` propose des outils de tri. Pourriez-vous obtenir
  le min et le max autrement ? Quel serait le coût ?
```

- [ ] **Step 10 : Écrire `correction.md`**

```markdown
# Correction — Exercice 2.1.1 Min, max et moyenne

## Démarche attendue

1. Lire `n` avec `clavier.nextInt()`, créer un tableau `int[n]`, puis le remplir
   avec une boucle `for`.
2. Initialiser `min` et `max` avec le **premier élément** du tableau (et non avec
   0 : sinon un tableau de valeurs négatives donnerait un maximum faux).
3. Parcourir le tableau une seule fois en mettant à jour `min`, `max` et la
   `somme`.
4. Calculer la moyenne en forçant un calcul en `double` :
   `(double) somme / n`.
5. Afficher les trois lignes, en formatant la moyenne avec
   `String.format(Locale.ROOT, "%.2f", moyenne)`.

## Points clés

- **Initialisation de min/max** : partir du premier élément, jamais de 0.
- **Division entière** : `somme / n` entre deux entiers tronque la partie
  décimale. Le `(double)` sur `somme` règle le problème.
- **Locale** : `%.2f` sans `Locale.ROOT` afficherait une virgule sur une machine
  configurée en français. On force le point pour une sortie reproductible.

## Erreurs fréquentes observées

- Initialiser `min`/`max` à `0` → résultat faux sur des tableaux entièrement
  positifs ou entièrement négatifs.
- Oublier le `(double)` → moyenne tronquée (`6` au lieu de `6.50`).
- Afficher la moyenne avec une virgule → échec des tests sur le format.

## Variantes possibles

- Cumuler la somme dans un `long` (fait dans la solution) pour éviter le
  débordement sur de grands tableaux. Pédagogiquement facultatif à ce stade.
- Utiliser une boucle `for` indexée plutôt qu'un `for-each` : équivalent.

## Pour approfondir

- Documentation de `java.util.Formatter` (motifs de `String.format`).
```

- [ ] **Step 11 : Commit**
```bash
git add exercises/module-2-tableaux-chaines-methodes/2.1.1-min-max-moyenne
git commit -m "feat(#18): exercice 2.1.1 min-max-moyenne"
```

---

## Task 2 : 2.1.2 — inversion

**Files (tous à créer)** sous `exercises/module-2-tableaux-chaines-methodes/2.1.2-inversion/` : mêmes 11 fichiers que Task 1, classe `Inversion`. Poms = BLOC-C/BLOC-D (ARTIFACT=`inversion`, NOM=`Inversion d'un tableau`). Utils = BLOC-A/BLOC-B. `evaluation.yml` = BLOC-E.

> **Tolérance espace final** (spec §3) : la sortie est sur une seule ligne ; les tests comparent après `strip()`, donc un espace final est toléré.

- [ ] **Step 1 : Arborescence + utils + poms + evaluation.yml** (cf. Task 1 Step 1, slug `2.1.2-inversion`).

- [ ] **Step 2 : Tests publics** — `tests/src/test/java/etnc/m2/InversionTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests publics de l'exercice 2.1.2 Inversion.
 * La sortie tient sur une ligne ; on compare apres strip() pour tolerer
 * un eventuel espace final.
 */
@DisplayName("Inversion — tests publics")
class InversionTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> Inversion.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("cas nominal : 1 2 3 4 -> 4 3 2 1")
    void cas_nominal() {
        assertThat(executer("4" + NL + "1 2 3 4" + NL).strip())
            .as("les éléments doivent être affichés dans l'ordre inverse")
            .isEqualTo("4 3 2 1");
    }

    @Test
    @DisplayName("trois éléments")
    void trois_elements() {
        assertThat(executer("3" + NL + "5 6 7" + NL).strip())
            .isEqualTo("7 6 5");
    }
}
```

- [ ] **Step 3 : Tests privés** — `tests-prives/src/test/java/etnc/m2/InversionPriveTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests privés de l'exercice 2.1.2 — cas limites.
 */
@DisplayName("Inversion — tests privés")
class InversionPriveTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> Inversion.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("un seul élément : reste identique")
    void un_seul_element() {
        assertThat(executer("1" + NL + "7" + NL).strip()).isEqualTo("7");
    }

    @Test
    @DisplayName("deux éléments")
    void deux_elements() {
        assertThat(executer("2" + NL + "8 9" + NL).strip()).isEqualTo("9 8");
    }

    @Test
    @DisplayName("valeurs négatives et doublons")
    void negatifs_et_doublons() {
        assertThat(executer("5" + NL + "-1 4 4 -1 0" + NL).strip())
            .isEqualTo("0 -1 4 4 -1");
    }
}
```

- [ ] **Step 4 : Starter** — `starter/src/main/java/etnc/m2/Inversion.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.2 — Inversion d'un tableau d'entiers.
 */
public class Inversion {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        // TODO : lire n (taille), puis n entiers dans un tableau int[]
        // TODO : afficher les éléments dans l'ordre INVERSE, sur une seule ligne,
        //        séparés par un espace (ex. : 1 2 3 4 -> 4 3 2 1)
    }
}
```

- [ ] **Step 5 : Vérifier l'échec du starter** — Run le `mvn -f .../2.1.2-inversion/starter/pom.xml test`. Attendu : **BUILD FAILURE**, `Tests run: 2, Failures: 2`.

- [ ] **Step 6 : Solution** — `solution/src/main/java/etnc/m2/Inversion.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.2 — Inversion d'un tableau (solution de reference).
 *
 * <p>On lit le tableau puis on le parcourt de la derniere case vers la premiere
 * en construisant la ligne de sortie. Un StringBuilder evite d'afficher un
 * espace final superflu.</p>
 */
public class Inversion {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        int n = clavier.nextInt();

        int[] valeurs = new int[n];
        for (int i = 0; i < n; i++) {
            valeurs[i] = clavier.nextInt();
        }

        StringBuilder ligne = new StringBuilder();
        for (int i = n - 1; i >= 0; i--) {
            ligne.append(valeurs[i]);
            if (i > 0) {
                ligne.append(' ');
            }
        }
        System.out.println(ligne);
    }
}
```

- [ ] **Step 7 : Vérifier la réussite de la solution** — Run le `mvn -f .../2.1.2-inversion/solution/pom.xml test`. Attendu : **BUILD SUCCESS**, `Tests run: 5, Failures: 0`.

- [ ] **Step 8 : `metadata.yml`**

```yaml
slug: inversion
titre: "Inversion d'un tableau"
module: 2
sous_groupe: "2.1"
position: 2
difficulte: facile
duree_estimee_min: 20
prerequis: []
objectifs_pedagogiques:
  - "Parcourir un tableau de la fin vers le début"
  - "Construire une ligne de sortie sans séparateur final superflu"
notions:
  - tableau
  - boucle
  - indexation
auteur: "ETNC"
version: 1
date_creation: 2026-06-02
```

- [ ] **Step 9 : `sujet.md`**

```markdown
# Exercice 2.1.2 — Inversion d'un tableau

## Contexte

Un message reçu doit parfois être relu à l'envers pour vérification. Vous allez
écrire un petit utilitaire qui inverse l'ordre d'une série de nombres.

## Énoncé

Écrivez un programme qui lit une série d'entiers et les réaffiche dans l'ordre
inverse.

La saisie se fait au clavier :

1. une première ligne contient `n`, le nombre de valeurs (au moins 1) ;
2. une seconde ligne contient les `n` entiers, séparés par des espaces.

Affichez les valeurs **dans l'ordre inverse**, sur une seule ligne, séparées par
un espace.

## Exemple

**Exécution attendue** (l'utilisateur saisit `4` puis `1 2 3 4`) :

```text
4 3 2 1
```

## Contraintes

- La classe doit s'appeler `Inversion` et rester dans le package `etnc.m2`.
- Lisez l'entrée avec un `Scanner`. Tout le code peut tenir dans `main`.
- Les valeurs sont séparées par un seul espace.

## Ce qui sera vérifié

- Votre programme compile et lit correctement la taille puis les valeurs.
- Les valeurs sont affichées dans l'ordre exactement inverse.
- Le programme fonctionne avec une seule valeur, deux valeurs, des doublons et
  des valeurs négatives.

## Pour aller plus loin (optionnel — non noté)

- Sauriez-vous inverser le tableau **en place** (en échangeant les cases), sans
  construire de nouvelle structure ? Combien d'échanges faut-il ?
```

- [ ] **Step 10 : `correction.md`**

```markdown
# Correction — Exercice 2.1.2 Inversion d'un tableau

## Démarche attendue

1. Lire `n`, créer et remplir le tableau (comme en 2.1.1).
2. Parcourir le tableau **de la dernière case (`n-1`) vers la première (`0`)**.
3. Construire la ligne de sortie au fur et à mesure, en n'ajoutant un espace
   qu'entre deux valeurs (pas après la dernière).

## Points clés

- **Sens de parcours** : la boucle décrémente l'indice (`for (int i = n-1; i >= 0; i--)`).
- **Séparateur** : ajouter l'espace seulement si `i > 0` évite l'espace final.
  (Les tests tolèrent un espace final, mais une sortie propre est préférable.)

## Erreurs fréquentes observées

- Boucler de `0` à `n-1` puis afficher → on n'inverse rien.
- Oublier le retour à la ligne final.
- Décaler les indices (`n` au lieu de `n-1`) → `ArrayIndexOutOfBoundsException`.

## Variantes possibles

- **Inversion en place** : échanger `valeurs[i]` et `valeurs[n-1-i]` pour `i`
  allant de `0` à `n/2`, puis afficher dans l'ordre normal.
- Utiliser `String.join` après avoir converti les entiers en chaînes.

## Pour approfondir

- Documentation de `StringBuilder` (construction efficace de chaînes).
```

- [ ] **Step 11 : Commit**
```bash
git add exercises/module-2-tableaux-chaines-methodes/2.1.2-inversion
git commit -m "feat(#18): exercice 2.1.2 inversion"
```

---

## Task 3 : 2.1.3 — recherche-lineaire

**Files** sous `.../2.1.3-recherche-lineaire/` : 11 fichiers, classe `RechercheLineaire`. Poms = BLOC-C/BLOC-D (ARTIFACT=`recherche-lineaire`, NOM=`Recherche lineaire`). Utils = BLOC-A/BLOC-B. `evaluation.yml` = BLOC-E.

- [ ] **Step 1 : Arborescence + utils + poms + evaluation.yml** (slug `2.1.3-recherche-lineaire`).

- [ ] **Step 2 : Tests publics** — `tests/src/test/java/etnc/m2/RechercheLineaireTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests publics de l'exercice 2.1.3 Recherche lineaire.
 */
@DisplayName("Recherche linéaire — tests publics")
class RechercheLineaireTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> RechercheLineaire.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("valeur présente : renvoie son indice (base 0)")
    void valeur_presente() {
        assertThat(executer("3" + NL + "10 20 30" + NL + "20" + NL))
            .as("20 est en indice 1")
            .isEqualTo("Indice : 1" + NL);
    }

    @Test
    @DisplayName("valeur absente : affiche Absent")
    void valeur_absente() {
        assertThat(executer("3" + NL + "10 20 30" + NL + "99" + NL))
            .isEqualTo("Absent" + NL);
    }
}
```

- [ ] **Step 3 : Tests privés** — `tests-prives/src/test/java/etnc/m2/RechercheLineairePriveTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests privés de l'exercice 2.1.3 — cas limites.
 */
@DisplayName("Recherche linéaire — tests privés")
class RechercheLineairePriveTest {

    private static final String NL = System.lineSeparator();

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> RechercheLineaire.main(new String[]{})));
        return sortie[0];
    }

    @Test
    @DisplayName("première position : indice 0")
    void premiere_position() {
        assertThat(executer("3" + NL + "7 8 9" + NL + "7" + NL))
            .isEqualTo("Indice : 0" + NL);
    }

    @Test
    @DisplayName("dernière position")
    void derniere_position() {
        assertThat(executer("3" + NL + "7 8 9" + NL + "9" + NL))
            .isEqualTo("Indice : 2" + NL);
    }

    @Test
    @DisplayName("doublons : renvoie la PREMIÈRE occurrence")
    void doublons_premiere_occurrence() {
        assertThat(executer("5" + NL + "4 2 4 2 4" + NL + "2" + NL))
            .isEqualTo("Indice : 1" + NL);
    }

    @Test
    @DisplayName("tableau à un élément, valeur absente")
    void un_element_absent() {
        assertThat(executer("1" + NL + "5" + NL + "6" + NL))
            .isEqualTo("Absent" + NL);
    }
}
```

- [ ] **Step 4 : Starter** — `starter/src/main/java/etnc/m2/RechercheLineaire.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.3 — Recherche lineaire dans un tableau.
 */
public class RechercheLineaire {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        // TODO : lire n (taille), puis n entiers dans un tableau int[]
        // TODO : lire la valeur recherchée
        // TODO : parcourir le tableau ; au PREMIER élément égal à la valeur,
        //        afficher  Indice : <i>   (i est l'indice, en commençant à 0)
        // TODO : si la valeur n'est jamais trouvée, afficher  Absent
    }
}
```

- [ ] **Step 5 : Vérifier l'échec du starter** — Run le `mvn -f .../2.1.3-recherche-lineaire/starter/pom.xml test`. Attendu : **BUILD FAILURE**, `Tests run: 2, Failures: 2`.

- [ ] **Step 6 : Solution** — `solution/src/main/java/etnc/m2/RechercheLineaire.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.3 — Recherche lineaire (solution de reference).
 *
 * <p>On parcourt le tableau du debut a la fin et on s'arrete des la premiere
 * case egale a la valeur cherchee. La variable {@code indice} vaut -1 tant que
 * rien n'a ete trouve, ce qui distingue le cas "absent".</p>
 */
public class RechercheLineaire {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        int n = clavier.nextInt();

        int[] valeurs = new int[n];
        for (int i = 0; i < n; i++) {
            valeurs[i] = clavier.nextInt();
        }
        int cible = clavier.nextInt();

        int indice = -1;
        for (int i = 0; i < n; i++) {
            if (valeurs[i] == cible) {
                indice = i;
                break;
            }
        }

        if (indice >= 0) {
            System.out.println("Indice : " + indice);
        } else {
            System.out.println("Absent");
        }
    }
}
```

- [ ] **Step 7 : Vérifier la réussite de la solution** — Run le `mvn -f .../2.1.3-recherche-lineaire/solution/pom.xml test`. Attendu : **BUILD SUCCESS**, `Tests run: 6, Failures: 0`.

- [ ] **Step 8 : `metadata.yml`**

```yaml
slug: recherche-lineaire
titre: "Recherche linéaire"
module: 2
sous_groupe: "2.1"
position: 3
difficulte: facile
duree_estimee_min: 20
prerequis: []
objectifs_pedagogiques:
  - "Parcourir un tableau à la recherche d'une valeur"
  - "S'arrêter à la première occurrence trouvée"
  - "Gérer le cas où la valeur est absente"
notions:
  - tableau
  - boucle
  - recherche
  - conditions
auteur: "ETNC"
version: 1
date_creation: 2026-06-02
```

- [ ] **Step 9 : `sujet.md`**

```markdown
# Exercice 2.1.3 — Recherche linéaire

## Contexte

Dans un fichier de fréquences, vous cherchez à quelle position apparaît une
valeur précise. La méthode la plus simple consiste à parcourir la liste du début
à la fin jusqu'à la trouver.

## Énoncé

Écrivez un programme qui recherche une valeur dans une série d'entiers et indique
sa position.

La saisie se fait au clavier :

1. une première ligne contient `n`, le nombre de valeurs (au moins 1) ;
2. une deuxième ligne contient les `n` entiers, séparés par des espaces ;
3. une troisième ligne contient la valeur recherchée.

Si la valeur est présente, affichez `Indice : <i>` où `i` est sa position, en
**commençant à compter à 0**. Si elle apparaît plusieurs fois, affichez l'indice
de la **première** occurrence. Si elle est absente, affichez `Absent`.

## Exemple

**Exécution attendue** (saisie `3`, puis `10 20 30`, puis `20`) :

```text
Indice : 1
```

## Contraintes

- La classe doit s'appeler `RechercheLineaire` et rester dans le package `etnc.m2`.
- Lisez l'entrée avec un `Scanner`. Tout le code peut tenir dans `main`.
- Les indices commencent à 0 (la première case du tableau est l'indice 0).

## Ce qui sera vérifié

- Le programme trouve la valeur quand elle est présente (début, milieu, fin).
- En cas de doublons, il renvoie bien la première occurrence.
- Il affiche `Absent` quand la valeur n'est pas dans le tableau.

## Pour aller plus loin (optionnel — non noté)

- Si le tableau était **trié**, connaissez-vous une méthode de recherche plus
  rapide ? (recherche dichotomique)
```

- [ ] **Step 10 : `correction.md`**

```markdown
# Correction — Exercice 2.1.3 Recherche linéaire

## Démarche attendue

1. Lire `n`, le tableau, puis la valeur cherchée.
2. Utiliser une variable `indice` initialisée à `-1` (« pas encore trouvé »).
3. Parcourir le tableau ; dès qu'une case vaut la cible, mémoriser l'indice et
   sortir de la boucle (`break`).
4. Après la boucle : si `indice >= 0`, afficher `Indice : <indice>` ; sinon
   `Absent`.

## Points clés

- **`break` à la première occurrence** : garantit qu'on renvoie la première
  position en cas de doublons, et évite de parcourir inutilement la suite.
- **Indice 0** : la première case est l'indice 0, comme partout en Java.
- **Sentinelle -1** : une valeur impossible comme indice sert à distinguer
  « trouvé » de « absent ».

## Erreurs fréquentes observées

- Continuer la boucle après avoir trouvé → renvoie la dernière occurrence.
- Afficher l'indice + 1 (réflexe « humain » de compter à partir de 1).
- Oublier le cas `Absent`.

## Variantes possibles

- Sans `break`, en parcourant à l'envers : renverrait la première occurrence
  aussi, mais c'est moins lisible.
- Extraire la recherche dans une méthode `static int indiceDe(int[], int)` —
  ce sera l'objet du sous-groupe 2.3 (méthodes).

## Pour approfondir

- Comparer recherche linéaire (O(n)) et recherche dichotomique (O(log n)).
```

- [ ] **Step 11 : Commit**
```bash
git add exercises/module-2-tableaux-chaines-methodes/2.1.3-recherche-lineaire
git commit -m "feat(#18): exercice 2.1.3 recherche-lineaire"
```

---

## Task 4 : 2.1.4 — rotation-matrice

**Files** sous `.../2.1.4-rotation-matrice/` : 11 fichiers, classe `RotationMatrice`. Poms = BLOC-C/BLOC-D (ARTIFACT=`rotation-matrice`, NOM=`Rotation d'une matrice`). Utils = BLOC-A/BLOC-B. `evaluation.yml` = BLOC-E.

> **Tolérance espace final** : sortie multi-lignes ; les tests normalisent chaque ligne avec `strip()` (helper `normaliser`).

- [ ] **Step 1 : Arborescence + utils + poms + evaluation.yml** (slug `2.1.4-rotation-matrice`).

- [ ] **Step 2 : Tests publics** — `tests/src/test/java/etnc/m2/RotationMatriceTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests publics de l'exercice 2.1.4 Rotation de matrice.
 * On normalise la sortie (strip par ligne) pour tolerer un espace final.
 */
@DisplayName("Rotation de matrice — tests publics")
class RotationMatriceTest {

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> RotationMatrice.main(new String[]{})));
        return sortie[0];
    }

    /** Recompose la sortie en lignes nettoyees (tolere espace final et type de saut de ligne). */
    private static String normaliser(String s) {
        StringBuilder sb = new StringBuilder();
        for (String ligne : s.strip().split("\\R")) {
            sb.append(ligne.strip()).append('\n');
        }
        return sb.toString();
    }

    @Test
    @DisplayName("matrice 2x2 tournée 90° horaire")
    void rotation_2x2() {
        String attendu = "3 1\n4 2\n";
        assertThat(normaliser(executer("2\n1 2\n3 4\n")))
            .as("[[1,2],[3,4]] tourné horaire -> [[3,1],[4,2]]")
            .isEqualTo(attendu);
    }

    @Test
    @DisplayName("matrice 3x3 tournée 90° horaire")
    void rotation_3x3() {
        String attendu = "7 4 1\n8 5 2\n9 6 3\n";
        assertThat(normaliser(executer("3\n1 2 3\n4 5 6\n7 8 9\n")))
            .isEqualTo(attendu);
    }
}
```

- [ ] **Step 3 : Tests privés** — `tests-prives/src/test/java/etnc/m2/RotationMatricePriveTest.java`

```java
package etnc.m2;

import etnc.util.CaptureEntree;
import etnc.util.CaptureSortie;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Tests privés de l'exercice 2.1.4 — cas limites.
 */
@DisplayName("Rotation de matrice — tests privés")
class RotationMatricePriveTest {

    private static String executer(String entree) {
        String[] sortie = new String[1];
        CaptureEntree.avecEntree(entree,
            () -> sortie[0] = CaptureSortie.capturer(() -> RotationMatrice.main(new String[]{})));
        return sortie[0];
    }

    private static String normaliser(String s) {
        StringBuilder sb = new StringBuilder();
        for (String ligne : s.strip().split("\\R")) {
            sb.append(ligne.strip()).append('\n');
        }
        return sb.toString();
    }

    @Test
    @DisplayName("matrice 1x1 : inchangée")
    void matrice_1x1() {
        assertThat(normaliser(executer("1\n5\n"))).isEqualTo("5\n");
    }

    @Test
    @DisplayName("valeurs négatives")
    void valeurs_negatives() {
        // [[-1, 2], [3, -4]] -> horaire -> [[3, -1], [-4, 2]]
        assertThat(normaliser(executer("2\n-1 2\n3 -4\n")))
            .isEqualTo("3 -1\n-4 2\n");
    }
}
```

- [ ] **Step 4 : Starter** — `starter/src/main/java/etnc/m2/RotationMatrice.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.4 — Rotation d'une matrice carree de 90 degres (sens horaire).
 */
public class RotationMatrice {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        // TODO : lire n (côté de la matrice), puis n*n entiers dans un int[n][n]
        // TODO : produire la matrice tournée de 90° dans le sens horaire
        // TODO : afficher la matrice résultat, une ligne par ligne,
        //        valeurs séparées par un espace
    }
}
```

- [ ] **Step 5 : Vérifier l'échec du starter** — Run le `mvn -f .../2.1.4-rotation-matrice/starter/pom.xml test`. Attendu : **BUILD FAILURE**, `Tests run: 2, Failures: 2`.

- [ ] **Step 6 : Solution** — `solution/src/main/java/etnc/m2/RotationMatrice.java`

```java
package etnc.m2;

import java.util.Scanner;

/**
 * Exercice 2.1.4 — Rotation d'une matrice carree (solution de reference).
 *
 * <p>Rotation de 90 degres dans le sens horaire. La ligne {@code i} de la
 * sortie correspond a la colonne {@code i} de la source, lue de bas en haut :
 * {@code resultat[i][j] = source[n-1-j][i]}. On construit toute la sortie dans
 * un StringBuilder pour eviter les espaces finaux superflus.</p>
 */
public class RotationMatrice {

    public static void main(String[] args) {
        Scanner clavier = new Scanner(System.in);
        int n = clavier.nextInt();

        int[][] matrice = new int[n][n];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                matrice[i][j] = clavier.nextInt();
            }
        }

        StringBuilder sortie = new StringBuilder();
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                sortie.append(matrice[n - 1 - j][i]);
                if (j < n - 1) {
                    sortie.append(' ');
                }
            }
            sortie.append(System.lineSeparator());
        }
        System.out.print(sortie);
    }
}
```

- [ ] **Step 7 : Vérifier la réussite de la solution** — Run le `mvn -f .../2.1.4-rotation-matrice/solution/pom.xml test`. Attendu : **BUILD SUCCESS**, `Tests run: 4, Failures: 0`.

- [ ] **Step 8 : `metadata.yml`**

```yaml
slug: rotation-matrice
titre: "Rotation d'une matrice"
module: 2
sous_groupe: "2.1"
position: 4
difficulte: moyen
duree_estimee_min: 40
prerequis: []
objectifs_pedagogiques:
  - "Déclarer et remplir un tableau à deux dimensions"
  - "Parcourir une matrice avec une double boucle"
  - "Calculer l'indice de destination d'une rotation de 90°"
notions:
  - matrice
  - tableau-2d
  - indexation
  - boucle
auteur: "ETNC"
version: 1
date_creation: 2026-06-02
```

- [ ] **Step 9 : `sujet.md`**

```markdown
# Exercice 2.1.4 — Rotation d'une matrice

## Contexte

Sur une carte numérique, faire pivoter une grille d'un quart de tour est une
opération courante. Vous allez faire tourner une matrice carrée de 90° dans le
sens des aiguilles d'une montre.

## Énoncé

Écrivez un programme qui lit une matrice carrée d'entiers et affiche cette
matrice tournée de **90° dans le sens horaire**.

La saisie se fait au clavier :

1. une première ligne contient `n`, le côté de la matrice (au moins 1) ;
2. les `n` lignes suivantes contiennent chacune `n` entiers, séparés par des
   espaces.

Affichez la matrice résultat : `n` lignes de `n` entiers, séparés par un espace.

## Exemple

**Exécution attendue** (saisie `2`, puis `1 2`, puis `3 4`) :

```text
3 1
4 2
```

La première colonne de départ (`1` en haut, `3` en bas) devient la première
ligne d'arrivée, lue de bas en haut : `3 1`.

## Contraintes

- La classe doit s'appeler `RotationMatrice` et rester dans le package `etnc.m2`.
- Lisez l'entrée avec un `Scanner`. Tout le code peut tenir dans `main`.
- La matrice est toujours **carrée** (`n` lignes de `n` colonnes).
- La rotation est de 90° dans le sens **horaire**.

## Ce qui sera vérifié

- Votre programme lit correctement `n` puis les `n × n` valeurs.
- La rotation horaire est correcte (testée sur 2×2 et 3×3).
- Les cas limites fonctionnent : matrice 1×1, valeurs négatives.

## Pour aller plus loin (optionnel — non noté)

- Que donne la rotation appliquée **quatre fois** de suite ? Vérifiez-le.
- Sauriez-vous faire la rotation **en place**, sans seconde matrice ? (plus
  difficile : penser aux échanges « en couches »)
```

- [ ] **Step 10 : `correction.md`**

```markdown
# Correction — Exercice 2.1.4 Rotation d'une matrice

## Démarche attendue

1. Lire `n`, puis remplir un tableau à deux dimensions `int[n][n]` avec une
   double boucle (`i` les lignes, `j` les colonnes).
2. Pour la rotation horaire, la case `resultat[i][j]` vaut `source[n-1-j][i]` :
   la ligne `i` de la sortie est la colonne `i` de la source, lue de bas en haut.
3. Afficher ligne par ligne, valeurs séparées par un espace.

## Points clés

- **Formule de rotation** : `resultat[i][j] = source[n-1-j][i]` (horaire).
  La retenir, c'est comprendre que les colonnes deviennent des lignes.
- **Double boucle** : la boucle externe parcourt les lignes de sortie, l'interne
  les colonnes.
- On peut écrire directement la sortie sans stocker `resultat` dans un second
  tableau (c'est ce que fait la solution).

## Erreurs fréquentes observées

- Confondre horaire et anti-horaire (`source[j][n-1-i]` donne l'anti-horaire).
- Inverser le rôle de `i` et `j` dans la lecture ou l'affichage.
- `ArrayIndexOutOfBoundsException` en oubliant le `n-1`.

## Variantes possibles

- **Transposer puis inverser les lignes** : une autre façon d'obtenir la
  rotation horaire, en deux étapes plus simples à visualiser.
- Stocker explicitement la matrice résultat avant l'affichage (plus lisible pour
  un débutant, à peine plus coûteux).

## Pour approfondir

- Les rotations en place « par couches » utilisées dans les exercices d'entretien.
```

- [ ] **Step 11 : Commit**
```bash
git add exercises/module-2-tableaux-chaines-methodes/2.1.4-rotation-matrice
git commit -m "feat(#18): exercice 2.1.4 rotation-matrice"
```

---

## Task 5 : Validation globale, lint, backlog et PR

**Files :**
- Modify: `docs/backlog.md` (cocher l'avancement #18 / sous-groupe 2.1)

- [ ] **Step 1 : Valider TOUTES les solutions du dépôt** — Run :
```bash
JAVA_HOME="E:/java/jdk-25.0.3+9" MVN="E:/java/apache-maven-3.9.9/bin/mvn" bash scripts/valider-solutions.sh
```
Attendu : le script se termine **exit 0**, toutes les solutions (module 1 + les 4 nouvelles de 2.1) passent.

- [ ] **Step 2 : Lancer le lint des exercices** — Run (le script scanne tout `exercises/`, sans argument) :
```bash
bash scripts/lint-exercices.sh
```
Attendu : `errors=0`. Le script vérifie pour chaque exo la présence des fichiers obligatoires (`sujet.md`, `metadata.yml`, `evaluation.yml`, `correction.md`, `starter/pom.xml`, `solution/pom.xml`), la validité YAML, les champs obligatoires de `metadata.yml`, et que la somme des poids `evaluation.yml` == `total`. Corriger tout écart signalé puis relancer.

- [ ] **Step 3 : Mettre à jour `docs/backlog.md`** — Ouvrir le fichier, repérer la ligne/section #18 et marquer le sous-groupe 2.1 comme livré (4 exercices). Respecter le format existant des autres entrées.

- [ ] **Step 4 : Commit du backlog**
```bash
git add docs/backlog.md
git commit -m "docs(backlog): #18 sous-groupe 2.1 (tableaux) livré"
```

- [ ] **Step 5 : Pousser la branche et ouvrir la PR**
```bash
git push -u origin feature/m2-exos-2-1
gh pr create --base main --title "feat(#18): exercices 2.1 (tableaux)" \
  --body "$(cat <<'EOF'
Sous-groupe 2.1 (Tableaux) du module 2 : 4 exercices.

- 2.1.1 min-max-moyenne
- 2.1.2 inversion
- 2.1.3 recherche-lineaire
- 2.1.4 rotation-matrice (90° horaire, carrée n×n)

Spec : docs/superpowers/specs/2026-06-02-m2-exos-2-1-design.md
Plan : docs/superpowers/plans/2026-06-02-m2-exos-2-1.md

Solutions de référence validées en local (publics + privés). Réf. backlog #18.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```
Attendu : PR créée. Vérifier ensuite la **CI verte** (4 jobs, dont `valider-solutions` et `lint-exercices`) avant de demander la revue.

---

## Notes d'exécution

- **Ordre strict** : Task 1 → 2 → 3 → 4 → 5. Task 1 sert de gabarit : une fois ses poms/utils validés, les suivants les réutilisent à l'identique.
- **Style (critère automatique)** : suivre le style module 1 — accolades systématiques même pour un `if` d'une ligne, pas d'`import *`, commentaires en français expliquant le *pourquoi*. Zéro warning Checkstyle/PMD attendu sur les solutions.
- **TDD** : pour chaque exo, on écrit d'abord les tests, on vérifie qu'ils échouent sur le starter (Step 5), puis on écrit la solution et on vérifie qu'elle passe (Step 7). C'est l'application du principe « la solution de référence est le test du format ».
- **Antériorité** : aucun exo n'utilise de méthode `static` écrite par le stagiaire ni de raccourci `java.util.Arrays` imposé — conforme à la spec §8.
```

