# Module 5 — sous-groupe 5.3 (Parsing et formats) Implementation Plan

> **For agentic workers:** Exécution **ultracode**. 1 agent produit 1 exo complet à partir de la spec §2.X ; le **pilote** relance le gate Maven, commit, clôture. Checkbox (`- [ ]`) pour le suivi.

**Goal:** Produire les 3 exercices du sous-groupe 5.3 (`import-csv-personnel`, `export-csv-trie`, `parseur-commandes`) sous `exercises/module-5-exceptions-io/`, conformes à la spec [`specs/2026-06-04-m5-exos-5-3-design.md`](../specs/2026-06-04-m5-exos-5-3-design.md). **Dernier sous-groupe → module 5 exos COMPLET.**

**Architecture:** Synthèse du module : CSV à la main (`split`/`String.join`, ch.7), I/O NIO.2 (ch.6), tri `Comparator` (M4), exceptions (5.1, pont formats↔exceptions). Modèle `enum Grade` + `record Personnel(nom, grade, anciennete)` **fourni complet** (« ne pas modifier ») dans 5.3.1 et 5.3.2 (copie locale par exo). 5.3.3 = texte pur (pas d'I/O). Calque de format : exos 5.1/5.2.

**Tech Stack:** Java 25, Maven, JUnit 5.11.0, AssertJ 3.26.3, build-helper. Package `etnc.m5`, groupId `etnc.piscine.m5`. Aucune dépendance ajoutée.

**Modèle de pom** : copie d'un pom 5.2/M5 (ex. `exercises/module-5-exceptions-io/5.2.1-compteur-lignes/{starter,solution}/pom.xml`). Starter ajoute `../tests` ; solution ajoute `../tests` + `../tests-prives`.

**Gate local (pilote, par exo)** :
```bash
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" -f exercises/module-5-exceptions-io/<exo>/solution/pom.xml test
JAVA_HOME="E:/java/jdk-25.0.3+9" "E:/java/apache-maven-3.9.9/bin/mvn" -f exercises/module-5-exceptions-io/<exo>/starter/pom.xml test-compile
```

---

## Task 1: 5.3.1 — `import-csv-personnel` (agent **Opus** — le plus subtil : CSV + validation + chaînage)

**Files :** `5.3.1-import-csv-personnel/` — `{starter,solution}/pom.xml`, `{starter,solution}/src/main/java/etnc/m5/{Grade,Personnel,ImportCsv}.java`, `tests/.../ImportCsvTest.java`, `tests-prives/.../ImportCsvPriveTest.java`, `metadata.yml`, `evaluation.yml`, `sujet.md`, `correction.md`.

- [ ] **Step 1 (agent Opus)** : spec §2 « 5.3.1 ». `Grade`/`Personnel` fournis complets (starter+solution). `ImportCsv.importer(Path csv) throws IOException` → `List<Personnel>` : readAllLines UTF-8, saut d'en-tête, ignore blancs, `split(",")` 3 champs, **toutes les lignes invalides → `IllegalArgumentException`** (cause chaînée pour grade via `Grade.valueOf` et ancienneté via `Integer.parseInt`). Starter `ImportCsv` `return List.of(); // TODO`. Tests `@TempDir` (≈8+8) : valide `containsExactly`, en-tête sauté, blancs ignorés, grade inconnu/ancienneté non-num/nb champs → IAE, `getCause()` instanceof (IAE / NumberFormatException), trim. Critère `demarche`. metadata position 1, difficulte moyen, notions [CSV, parsing, split, record, enum, exception, chainage].
- [ ] **Step 2 (pilote)** : gate Maven.
- [ ] **Step 3 (pilote)** : commit `feat(#21): exercice 5.3.1 import-csv-personnel (CSV -> records, exceptions chainees)`.

---

## Task 2: 5.3.2 — `export-csv-trie` (agent Sonnet)

**Files :** `5.3.2-export-csv-trie/` — `{Grade,Personnel,ExportCsv}.java` (starter+solution), tests, meta.

- [ ] **Step 1 (agent Sonnet)** : spec §2 « 5.3.2 ». `Grade`/`Personnel` fournis. `ExportCsv.exporter(List<Personnel> personnels, Path csv) throws IOException` : trie une **copie** par `Comparator.comparingInt(Personnel::anciennete).reversed().thenComparing(Personnel::nom)`, écrit en-tête `nom,grade,anciennete` + 1 ligne/personnel (`String.join(",", nom, grade().name(), ""+anciennete)`) via `Files.write(csv, lignes, UTF_8)` ; **ne mute pas la source**. Starter corps vide. Tests `@TempDir` (≈8+8) : relecture triée, format exact, liste vide → en-tête seul, source non mutée, égalité ancienneté → nom, écrasement, UTF-8. Critère `idiomatisme`. metadata position 2, difficulte moyen, notions [CSV, Comparator, String.join, Files.write, record].
- [ ] **Step 2 (pilote)** : gate Maven.
- [ ] **Step 3 (pilote)** : commit `feat(#21): exercice 5.3.2 export-csv-trie (Comparator + String.join + Files.write)`.

---

## Task 3: 5.3.3 — `parseur-commandes` (agent Sonnet — texte pur, pas d'I/O)

**Files :** `5.3.3-parseur-commandes/` — `ParseurCommandes.java` (starter+solution), tests, meta (pas de modèle Grade/Personnel ; pas de `@TempDir`).

- [ ] **Step 1 (agent Sonnet)** : spec §2 « 5.3.3 ». `ParseurCommandes.executer(String ligne)` → `int` : garde `isBlank` → IAE « commande vide » ; `ligne.trim().split("\\s+")` ; `switch` sur la commande (`somme`/`diff`/`produit` 2 args, `oppose` 1 arg) ; commande inconnue → IAE ; mauvais nombre d'arguments → IAE ; argument non numérique → `NumberFormatException` (parseInt propagée). Starter `return 0; // TODO`. Tests (≈8+8, SANS @TempDir) : opérations correctes, inconnue/manquant/en-trop → IAE, non-numérique → NumberFormatException, espaces multiples/négatifs/casse. Critère `idiomatisme`. metadata position 3, difficulte moyen, notions [parsing, split, switch, dispatch, exception].
- [ ] **Step 2 (pilote)** : gate Maven.
- [ ] **Step 3 (pilote)** : commit `feat(#21): exercice 5.3.3 parseur-commandes (split + dispatch + exceptions)`.

---

## Task 4: Clôture — validation, lint, backlog (module 5 COMPLET), PR, merge

- [ ] **Step 1 (pilote)** : `git clean -fdX exercises/module-5-exceptions-io/ ; bash scripts/lint-exercices.sh` → 0 erreur (58 exos attendus).
- [ ] **Step 2 (pilote)** : MAJ `docs/backlog.md` (#21 : ✅ 5.3 → **volet exercices COMPLET** ; envisager de marquer #21 globalement comme « exos terminés, reste fiche Projet binôme #2 ») ; commit.
- [ ] **Step 3 (pilote)** : `git push -u origin feature/m5-exos-5-3` + `gh pr create` (titre `feat(#21): module 5 — sous-groupe 5.3 (parsing et formats)`).
- [ ] **Step 4 (pilote)** : `gh pr checks <n> --watch` → 4 jobs verts.
- [ ] **Step 5 (pilote)** : `gh pr merge <n> --merge --delete-branch` ; `git checkout main && git pull`.

---

## Self-review (rédacteur du plan)

- **Couverture spec** : §2 5.3.1→Task 1, 5.3.2→Task 2, 5.3.3→Task 3 ; §0/§1 (modèle fourni, CSV à la main) → briefs ; §3 vérif → gates + Task 4 ; §5 critères → Task 4. ✅
- **Placeholders** : `// TODO`/`return …` = stubs ; renvois « spec §2.X » délibérés (ultracode). ✅
- **Cohérence noms** : `ImportCsv.importer` ; `ExportCsv.exporter` ; `ParseurCommandes.executer` ; `Grade`/`Personnel(nom,grade,anciennete)`. Critères : demarche / idiomatisme / idiomatisme. ✅
- **Points d'attention** : (a) modèle `Grade`/`Personnel` **identique** entre starter et solution (« ne pas modifier ») ; (b) 5.3.1 chaîne la cause (grade→IAE, ancienneté→NumberFormatException) ; (c) tri 5.3.2 = ancienneté décroissante puis nom, **copie défensive** (source non mutée) ; (d) 5.3.3 garde `isBlank` AVANT split ; (e) `split(",")`/`split("\\s+")` — CSV simple sans virgule dans un champ.

---

*Plan rédigé le 2026-06-04. Spec : [`specs/2026-06-04-m5-exos-5-3-design.md`](../specs/2026-06-04-m5-exos-5-3-design.md). Exécution **ultracode**. Dernier sous-groupe du module 5.*
