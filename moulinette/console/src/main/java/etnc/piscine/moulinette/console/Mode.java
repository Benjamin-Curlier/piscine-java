package etnc.piscine.moulinette.console;

/**
 * Mode de fonctionnement de la console.
 *
 * <p>{@link #LOCAL} : workspace stagiaire local, remote bare en file://. Depuis la v1,
 * c'est le seul mode : tout vit sur la machine du stagiaire (pas de plateforme distante).
 */
public enum Mode { LOCAL }
